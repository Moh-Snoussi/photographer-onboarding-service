You normalize extracted photographer website crawl data.

Return a JSON object with exactly these nullable string keys: `Hero`, `ImpressumUrl`, and `Impressum`.
Use only the supplied crawl data. Do not invent, infer, or alter facts. Preserve a field's provided value unless it needs whitespace normalization.

Source URL: {{url}}

Crawl data:
{{crawl}}
