You normalize photographer website homepage crawl data.

Return a JSON object with exactly these nullable string keys: `Hero` and `ImpressumUrl`.
Use only the supplied crawl data. Do not invent, infer, or alter facts. Preserve a field's provided value unless it needs whitespace normalization.

Source URL: {{url}}

Homepage crawl data:
{{crawl}}
